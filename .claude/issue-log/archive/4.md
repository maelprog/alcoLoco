# Journal — #4 Squelette de l'application Angular : routing, layout, client HTTP

Démarrée le 2026-08-21. Dépendance #1 mergée ; `main` à `33f1c69`. Première issue front de la file :
elle n'hérite d'aucune dette écrite ailleurs, mais elle pose les fondations que #8, #9, #12, #19 à
#28 réutiliseront.

## Portée telle que comprise au lancement

Routing (sélection de profil, profil, événements), layout applicatif, client HTTP typé, état du
profil courant sans authentification, gestion des erreurs API. Critères d'acceptation : routes
navigables avec écrans vides, et `GET /health` consommé et affiché en dev.

## Arbitrages d'orchestration rendus au lancement

- **Client HTTP typé : écrit à la main, pas généré.** L'issue dit « idéalement généré depuis
  l'OpenAPI » — c'est une préférence, pas un critère d'acceptation. Générer imposerait une
  dépendance et une étape de build supplémentaires, et le document OpenAPI de #3 n'enregistre
  aujourd'hui **aucun chemin métier** (cf. dettes léguées dans `issue-log/6.md` et `15.md`) : il n'y
  aurait presque rien à générer. Décision : types écrits à la main, alignés sur les conventions
  d'API du briefing (`snake_case`, RFC 7807, dates ISO 8601 `Z`). À rouvrir quand les chemins
  métier existeront, pas ici.
- **Gestion d'erreur** : le front doit savoir lire une réponse `application/problem+json` et
  s'appuyer sur le champ `type`, **jamais** sur `title` (chaînes d'API en anglais, le front porte le
  texte affiché — arbitrage du 2026-08-20 au briefing).

## Contrainte connue au lancement

Gates 4 et 5 seuls (`npm run lint`, `npm run test -- --watch=false`). Les gates Rust 1 à 3 ne
s'appliquent pas si l'issue ne touche aucun Rust — à **déclarer** non applicables dans la PR, pas à
passer sous silence.

## Décisions / constats

_(à compléter au fil des allers-retours)_

## Vérification 1 — PR #46 (2026-08-21)

PR #46 ouverte sur `feat/4-angular-app-skeleton`, tête `75eb016`, 33 fichiers **tous sous `web/`**,
aucune dépendance ajoutée d'après l'auteur. Gates 4 et 5 rapportés verts par l'auteur ; gates 1 à 3
déclarés non applicables (diff sans Rust), le job `rust` de la CI ayant tourné vert de son côté.

Points transmis au vérificateur comme incertains par l'auteur : chemins de routes en anglais face à
des libellés français ; `API_BASE_URL = '/api'` supposant un reverse proxy non décrit dans le dépôt ;
un test dépendant du fuseau ; `post`/`put`/`delete` du client jamais confrontés à l'API réelle ;
conformité à l'arbitrage « client écrit à la main » (l'agent n'avait pas `issue-log/4.md` dans son
arbre, celui-ci n'ayant été commité qu'en `6716bec`) ; proxy éprouvé contre un bouchon, pas contre
l'API.

### Verdict de la vérification 1 — un bloquant, sans changement de code

Le vérificateur a reproduit sur son propre export : CI verte sur le sha de tête `75eb016` (aucun
commit d'appoint), 33 fichiers tous sous `web/`, `package.json`/`package-lock.json` **byte-identiques**
(dépendances : zéro, prouvé mécaniquement), zéro suppression de test, aucun `eslint-disable` /
`@ts-ignore` / `.skip` / `.only`, aucun trailer interdit. Gates 4 et 5 rejoués : `All files pass
linting.` et 46/46. Les **cinq mutations de l'auteur reproduites une à une**, même nombre de rouges
et mêmes tests nommés. Sonde dans les deux sens sur le garde-fou de navigation : il rougit sur un
lien sans route, il repasse au vert quand la route existe — il ne refuse pas tout. Contrat backend
recompté contre les sources **et contre le binaire réel** (404 et 405 servis par une instance de
l'API, injectés dans les vrais modules du front).

**BLOQUANT B1** — corps de PR, section « Hors gates » : `260,85 kB initiaux` est faux. Mesure du
vérificateur, rejouée après `rm -rf .angular dist` avec hash de contenu identique : **266,67 kB**,
base `56c6f56` à **189,36 kB**, delta réel **+77,31 kB**. Écart de 5,82 kB **dans le sens optimiste**.
Retenu bloquant non pour sa taille mais parce qu'il est écrit au présent dans la section qui
revendique la mesure, et qu'il servira de ligne de base aux PR front suivantes. Remède : une ligne du
corps de PR, sans toucher au code ni relancer la CI — le sha de tête reste `75eb016`.

### Dettes à reporter (ne pas traiter dans #4)

- **M1** → `web/src/app/core/api/page.ts:12,15` : `DEFAULT_PAGE_LIMIT`/`MAX_PAGE_LIMIT` recopiés de
  `crates/api/src/pagination.rs` sans lien mécanique. Mutation `999`/`500` → lint vert, **46/46 vert**.
  `DEFAULT_PAGE_LIMIT` n'est référencé nulle part : constante morte, muette par construction.
  → à fermer dans la première issue qui consomme une collection paginée (**#15**, ou #8 si elle
  pagine `GET /profiles`).
- **M2** → `web/src/app/core/api/problem-details.ts:39-52` : les six identifiants `type` peuvent
  dériver du backend sans qu'un test rougisse (mutation sur deux d'entre eux → **46/46 vert**). Le
  test `api-error.spec.ts:88` « every problem kind the API mints » énumère les constantes **du front**,
  pas celles de l'API : il ne peut détecter ni une valeur dérivée ni un septième type. → un test lisant
  `crates/api/src/error.rs` ou le document OpenAPI fermerait les deux ; à poser avec **#6**.
- **m1** (mineur, non retenu) → `health-indicator.spec.ts:61` : sous `TZ=UTC` — le cas de la CI — le
  test ne garantit que « pas la chaîne ISO brute », jamais « dans le fuseau du lecteur ». Sous
  `TZ=Europe/Paris` : 1 échec sur ce test, suite verte par ailleurs. Fixer un fuseau explicite le
  rendrait mordant partout.
- **m3** (mineur) → `api-error.interceptor.ts:10` intercepte **toute** requête `HttpClient`, pas
  seulement celles sous `API_BASE_URL`. Sans effet tant qu'`ApiClient` est seul appelant.
- **Non vérifié, à tracer** : `API_BASE_URL = '/api'` suppose un reverse proxy de production que
  **rien dans le dépôt ne décrit**. Ce n'est pas un défaut de #4 ; mérite le README de déploiement ou
  l'issue de packaging.

### Aller-retour 1 — clos, sans changement de code

L'auteur a **rebâti lui-même** les deux révisions plutôt que de recopier les chiffres du
vérificateur : tête `75eb016` → 266,67 kB (hash de contenu `3D4OU3IP`, le même que le vérificateur),
base `56c6f56` → 189,36 kB, delta +77,31 kB. Aucune divergence entre les deux mesures indépendantes.

**Origine de l'erreur, telle qu'établie par l'auteur** : son `npm run build` datait de 09:44:43,
l'extraction de `NAV_LINKS` et son test de couplage nav↔routes de 09:51. Il avait donc publié une
mesure **réelle mais d'un arbre antérieur**, présentée comme celle de `75eb016`. Le motif est
générique et vaut d'être retenu : *une mesure ne vaut que datée du dernier changement de code.*

Corps de PR corrigé par `gh api -X PATCH …/pulls/46` — ⚠ **`gh pr edit` échoue sur ce dépôt**
(`GraphQL: Projects (classic) is being deprecated … repository.pullRequest.projectCards`), même
erreur que `gh issue view` sans `--json`. Le `PATCH` via `gh api` fait le même travail sur le corps.
La ligne porte les trois chiffres et **garde trace du 260,85 kB faux avec son motif** au lieu de
l'effacer.

Contrôlé par l'orchestrateur **sur l'artefact, pas sur le rapport** : `.head.sha` toujours
`75eb0166ff10716483af3056e076f33300756437`, corps relu par `gh api`, jobs `rust` et `web` toujours
verts sur ce sha. Le code vérifié n'a pas bougé d'un octet — le verdict conditionnel du vérificateur
(« une fois cette ligne corrigée, rien ne s'oppose au merge ») s'applique tel quel, sans re-vérifier
un arbre identique.

### Issue close

PR #46 mergée le 2026-08-21 en squash `e614a81`. 2 gates verts (4 et 5), gates 1-3 non applicables
(motif vérifié mécaniquement : 0 fichier Rust dans le diff), 1 aller-retour.
