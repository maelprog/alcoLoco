# Issue #1 — Bootstrap du dépôt : workspace Rust, app Angular, Postgres local

Milestone V1, label `infra`. Aucune dépendance. Tête de file.

## Périmètre (corps de l'issue)

Workspace Cargo (`api` binaire axum + `domain` bibliothèque), app Angular dans `web/`,
`docker-compose.yml` avec Postgres, README (prérequis + commandes), CI minimale
(`cargo fmt --check`, `cargo clippy`, `cargo test`, `ng lint`, `ng test`).

Critères : `docker compose up` démarre Postgres ; le back répond sur un port local ; le
front sert une page vide ; la CI passe sur une PR vide.

## Contexte transmis à l'agent A

- Les gates du briefing sont **provisoires** : c'est cette issue qui crée la CI. Les gates
  locaux (conteneurs) s'appliquent ; le job CI n'existe pas encore et ne pourra être vert
  qu'après le premier push de cette branche.
- `SPEC_CORRECTED.md` n'existe pas → lire `SPEC.md` §2 et §9.
- Branche `chore/1-bootstrap-repo` (travail d'outillage, pas de fonctionnalité produit).

## Journal

- **2026-08-17** — socle #38 mergé, `main` propre à `36b3179`, aucun worktree résiduel,
  aucune PR ouverte. Agent A lancé.
- **2026-08-17** — Agent A rendu. **PR #39** ouverte sur `chore/1-bootstrap-repo`,
  tête `c41375e`, 2 commits (scaffold + bump des actions GitHub). Worktree
  `.claude/worktrees/agent-afd0a74a4cd75503d`, agent id `afd0a74a4cd75503d`.
  Vérification (agent B) lancée — **deux échecs API 529 côté serveur avant démarrage**,
  relancée. Aucun retour de vérification à ce stade.
- **2026-08-17 15:51 UTC** — **PR #39 mergée** (squash, `5a9f96f`), issue #1 fermée. CI
  verte : jobs `rust` (13:49:03→13:49:34 UTC) et `web` (13:49:03→13:49:22 UTC).
- **2026-08-18** — état reconstitué depuis GitHub après perte du contexte orchestrateur.
  ⚠ **Le rapport de vérification de l'agent B est perdu** : aucun commentaire n'a été
  posté sur la PR, et le merge a eu lieu ~1 h 50 après la dernière écriture de ce journal.
  Les points ci-dessous ont donc été **repris à la main sur `main`**, pas validés par B.
  Nettoyage fait : worktree `agent-afd0a74a4cd75503d` supprimé, branches locales
  `chore/1-bootstrap-repo`, `docs/spec-corrections` et `worktree-agent-afd0a74a4cd75503d`
  supprimées (contenu confirmé identique à `main`), `main` local à `5a9f96f`.
  La branche **distante** `origin/chore/1-bootstrap-repo` n'a pas été supprimée.

### Traitement des 10 points le 2026-08-18

| # | Sort |
|---|---|
| 1 | `ingested_alcohol_grams` + `ETHANOL_DENSITY_G_PER_ML` : formule et constante **relues et conformes** à SPEC §6.1 (`250 mL × 5 % → 9,8625 g`, valeur du test). Conservés ; le briefing dit désormais que **#16 les réutilise sans les réécrire**. |
| 2 | `resolver = "3"` / `edition = "2024"` : **conservés** (mergés), inscrits au briefing § *Dépendances* pour que les crates suivants en héritent. |
| 3-5 | `web/README.md` supprimé, une ligne du `README.md` racine modifiée, gabarit Angular remplacé et `lang="fr"` : **acceptés de fait** par le merge, sans relecture ligne à ligne. |
| 6 | `cargo audit` / `npm audit` : **absence confirmée** dans `.github/workflows/ci.yml`. Actée comme dette assumée ; leur ajout passe par l'*Escalade* (modification de la CI). |
| 7 | Toolchain `1.97.1` épinglée par `env: RUST_TOOLCHAIN` du workflow, **aucun `rust-toolchain.toml`** : confirmé. Inscrit au briefing § *Gates* — l'alignement avec `alcoloco-rust:dev` n'est pas garanti mécaniquement. |
| 8 | Gate 5 périmé : confirmé (`npm run test` → `ng test`, vitest + jsdom). Le briefing porte la commande exacte de la CI, `--browsers=ChromeHeadless` retiré. |
| 9 | `0 ignored` : **normal à ce stade** — aucun test `#[ignore]` n'existe encore, cette famille naît avec #5. Rien à recompter. |
| 10 | `chown -R 1000:1000` du volume `alcoloco-cargo` : inscrit au briefing § *Pièges connus* (n° 7), avec la commande de remise en état. |

### Points signalés par A à la vérification (non validés)

1. `crates/domain` porte déjà `ingested_alcohol_grams` + `ETHANOL_DENSITY_G_PER_ML`
   (§6.1) — empiétement possible sur #16, et justesse du calcul à recontrôler.
2. `resolver = "3"` + `edition = "2024"` — engagent le projet, hors spec.
3. `web/README.md` (boilerplate anglais du CLI) supprimé.
4. Une ligne de contenu préexistant du `README.md` racine modifiée.
5. Gabarit d'accueil Angular remplacé, `index.html` en `lang="fr"`.
6. `cargo audit` / `npm audit` **exclus** de la CI — tranche le `TODO` du briefing.
7. Toolchain Rust épinglée à `1.97.1` dans `ci.yml`, sans `rust-toolchain.toml`.
8. **Le gate 5 du briefing est périmé** : lanceur vitest + jsdom (défaut Angular 22),
   donc plus de `--browsers=ChromeHeadless`.
9. `0 ignored` au `cargo test --workspace` — à recompter.
10. Volume partagé `alcoloco-cargo` : A a dû le `chown -R 1000:1000` pour que la
    recette du briefing marche avec `--user`. Concerne tous les agents suivants.

### Jobs CI annoncés par A (à confirmer par B avant de figer le briefing)

Workflow `CI` dans `.github/workflows/ci.yml`, sur `pull_request` et `push` vers `main`.
Jobs `rust` et `web`, tous deux `ubuntu-latest`.

## Dette léguée

- Les trois `TODO` du briefing ouverts par cette issue sont **soldés le 2026-08-18** en
  lisant `.github/workflows/ci.yml` : jobs `rust` et `web` ; commandes exactes des cinq
  gates ; `cargo audit` / `npm audit` absents et actés comme dette.
- **Reste ouvert** : la CI n'a aucun audit de sécurité des dépendances, et la version de
  toolchain est épinglée à deux endroits sans lien mécanique (`env` du workflow ↔ image
  `alcoloco-rust:dev`).
- `TODO` du briefing encore ouvert, hors périmètre de #1 : citer des fichiers de tests de
  référence une fois #3 et #16 mergées.
