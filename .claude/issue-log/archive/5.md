# Journal — #5 Décisions de calcul : arbitrages actés et cas de référence

## Arbitrage orchestrateur/utilisateur du 2026-08-21 — périmètre de code

L'énoncé était ambigu au sens du §7 du skill : l'issue exige des tests Rust `#[ignore]`
(donc du code à tester), mais #16 est titrée « Widmark, ingestion, absorption, élimination ».
Trois lectures ont été présentées à l'utilisateur, qui a tranché pour **le socle calculable** :

**#5 implémente dans `crates/domain`** :
- Watson TBW homme / femme (§6.2), repli Widmark `r` = 0,68 / 0,55 si taille ou âge manquant ;
- `C₀ = 0,806 × A / TBW` ;
- le profil trapézoïdal `R(τ)` de §6.4 et ses dégénérescences (`a=0` ou `b=0` → rampe ;
  `a=b=0` → Widmark pur ; `a=b` → triangle) ;
- tests **actifs** pour les cas indépendants du modèle d'absorption ;
- tests **`#[ignore]`** pour tout ce qui porte sur la forme de la courbe avant le pic.

**#16 conserve** : le trait `AbsorptionProfile`, la superposition `Σ Rᵢ`, l'intégration en
avant à pas fixe et le plancher à zéro.

Motif retenu : c'est la seule lecture qui satisfait le texte d'acceptation de l'issue
(« Ils apparaissent ainsi à chaque `cargo test` » impose des tests exécutables), et le
briefing acte déjà le précédent — #1 a posé `ingested_alcohol_grams`, morceau du périmètre
de #16, que #16 « réutilise, ne réécrit pas ».

## Constat bloquant levé avant délégation — coefficient de Watson

Le corps de l'issue porte un bloc « Mise à jour du 2026-08-17 (PR #38) » qui affirme que la
valeur publiée est **`0,09156`** et en dérive TBW = 45,93 L et C₀ = 0,346 g/L.

**Ce bloc est faux.** `SPEC.md` §6.2 retient `0,09516` (article d'origine, *Am J Clin Nutr*
1980;33:27-39) et désigne explicitement `0,09156` comme la variante transposée qui circule sur
les calculateurs grand public. Vérifié cohérent de bout en bout : §10.1 pose le même cas de
comparaison (homme 80 kg / 180 cm / 30 ans) et en tire **TBW = 45,82 L** et **C₀ = 0,347 g/L**,
ce que `0,09516` reproduit exactement (2,447 − 2,8548 + 19,332 + 26,896 = 45,8202 ;
0,806 × 19,725 / 45,8202 = 0,34697).

Conforme à la note du briefing : « les corps des issues #5, #16 et #37 recopient l'ancien
coefficient et l'ancien tableau — c'est `SPEC.md` qui fait foi ». **SPEC.md gagne**, et l'écart
se signale dans la PR.

## Divergence mineure repérée, à trancher par le calcul

Variante « rampe linéaire » de §10.1 : `SPEC.md` dit **0,222 g/L**, le corps de l'issue dit
**0,221 g/L**. Agent A doit **dériver** la valeur plutôt que recopier l'une des deux, et
rapporter laquelle tombe juste.

> **Correction du 2026-08-23** — la rédaction initiale de ce paragraphe attribuait aussi `0,221`
> au « briefing orchestrateur ». **C'est faux** : `grep` sur `.claude/orchestrator.md` ne rend
> **ni `0,221` ni `0,222`** (revérifié par l'orchestrateur, pas repris du rapport). La valeur
> `0,221` ne vivait que dans le corps de l'issue #5 et dans ce journal. Le briefing n'a jamais
> porté cette erreur — ne pas partir la corriger là-bas.
>
> **Tranché par le calcul** : `0,222` est juste. La valeur exacte est `0,22197251430591752`, dont
> `0,222` est l'**arrondi** et `0,221` la **troncature**. `SPEC.md` §10.1 avait raison.

## Dette héritée

- `issue-log/archive/1.md` §9 : « `0 ignored` est normal à ce stade — cette famille de tests
  naît avec #5 ». C'est donc #5 qui doit faire passer le compte de tests ignorés de 0 à N.

---

## 2026-08-21 — PR #48 ouverte, VÉRIFICATION NON FAITE

**PR #48** `feat(domain): fige les cas de référence du calcul d'alcoolémie (#5)`
- branche `feat/5-calculation-reference-cases`, tête **`965f7593d4e79a303fd5e25a697f7fa591415f50`**
- état `OPEN`, `MERGEABLE`
- **CI verte sur la tête**, vérifié par l'orchestrateur lui-même via `gh pr checks 48` (pas
  repris d'un rapport d'agent) : job `rust` pass 1 min 14 s, job `web` pass 28 s,
  run `32473861728`.

⚠ **L'agent de vérification (`verify-pr`) est mort au démarrage** — `API error: session limit`,
reset annoncé à 16 h 20 Europe/Paris. Il n'a rendu **aucun constat** : son seul message est
« I'll start by reading the project briefing ». **Rien n'a été vérifié.**

**NE PAS MERGER #48 en l'état.** La CI verte ne remplace pas la vérification : le briefing acte
qu'aucun gate ne couvre les valeurs chiffrées, et #5 ne produit que des valeurs chiffrées.

### À relancer à la reprise

Relancer un agent `verify-pr` **neuf** sur PR #48 / issue #5, avec le contrat complet de
`references/agent-b.md`. Gamme Postgres réservée à B : `alcoloco-5-b` / port `5505` (a priori
inutile, `domain` est sans I/O — à confirmer, pas à supposer).

Points que l'auteur a signalés comme incertains, à retransmettre **sans son raisonnement** :
1. Repli Widmark sur âge **ou** taille manquante, alors que l'équation féminine de Watson n'a pas
   de terme d'âge — lecture littérale de §6.2, arbitrage de l'auteur, à juger.
2. Valeur de `R(τ)` en `τ = 0` quand `a = b = 0` (Dirac) — conservation de la dose à vérifier.
3. Fonction `absorbed_alcohol_grams` ajoutée à côté de `R(τ)` — élargissement d'API, empiète-t-elle
   sur #16 ?
4. Interprétation de « valeur à mi-montée ».
5. Le premier des deux commits serait rouge par construction (trace TDD) — à falsifier sur
   l'historique.

Points chiffrés à établir **indépendamment** (c'est le cœur : #16 s'adossera à ces valeurs) :
coefficient Watson `0,09516` vs `0,09156` ; ligne « rampe linéaire » de §10.1 (`SPEC.md` 0,222 vs
briefing/issue 0,221) ; toutes les autres lignes du tableau §10.1 ; compte réel
`passed`/`failed`/`ignored` et motif de chaque `#[ignore]` (doit être #37, jamais une commodité).

### Worktree resté en place

`/home/yallo/projects/alcoLoco/.claude/worktrees/agent-af590bd83d1e2e145`
(branche `worktree-agent-af590bd83d1e2e145`). À nettoyer après merge, pas avant.

---

## 2026-08-23 — vérifiée puis mergée

Agent `verify-pr` **neuf** relancé en contexte vierge (le précédent était mort sur limite de
session sans rendre un constat). Contrat complet d'`agent-b.md`, sans le raisonnement de A.

**PR #48 mergée** en squash — commit **`add113a`** sur `main`. Issue #5 fermée automatiquement.
Branche distante supprimée ; worktree, branche locale et ref distante périmée nettoyés par
l'orchestrateur (`git worktree list` → `main` seul).

**Aucun constat BLOQUANT.** 2 MOYENS, 4 MINEURS — tous propagés, cf. plus bas.

### Ce que la vérification a réellement établi

Le vérificateur a **dérivé les chiffres depuis `SPEC.md` §6 seul, avant de lire le code** :
les **24 constantes assertées tombent au bit près**. Deux recoupements indépendants :
quadrature à 2 000 000 pas sous `R` → `19.724999999819` contre `A = 19,725` ; balayage à
10⁶ points du maximum de `C(τ)` → `0,22737640055824` à 45,6769 min, concordant avec la forme
fermée.

- **Coefficient de Watson** : le code implémente bien `0,09516` (`crates/domain/src/lib.rs:87`),
  celui de `SPEC.md` §6.2. Les six chiffres du bloc « Mise à jour du 2026-08-17 » de l'issue
  restent faux — cf. *À propager*.
- **Rampe linéaire** : `0,222` juste, cf. la correction plus haut.
- **Tableau §10.1** : les quatre lignes de `SPEC.md` sont justes (Widmark pur 0,347 ; trapèze
  0,227 / 45,7 min ; rampe 0,222 ; exponentiel 0,256 / 26 min).
- **Tests** : `77 passed / 0 failed / 4 ignored`, rejoués sur `git archive 965f759` en
  `CARGO_TARGET_DIR` isolé. Le compte d'ignorés passe bien de **0 à 4** — la dette de
  `archive/1.md` §9 est soldée. Les 4 portent la chaîne exacte
  `#[ignore = "modèle d'absorption non arbitré — cf. #37"]` (`lib.rs:850, 911, 944, 976`) et sont
  tous **verts sous `-- --ignored`** : aucun n'est une commodité.
- **CI** : run `32473861728`, `head_sha` = tête de PR exacte, `rust` et `web` `success`.
- **Trace TDD falsifiée sur l'historique** : `cargo test -p domain` sur `1ac347f` rend
  `4 passed; 21 failed; 4 ignored`, exactement l'annonce.
- **10 sondes de mutation** choisies par le vérificateur : **8 mordent, 1 survit** (MOYEN-1),
  1 faux positif de sa propre regex qu'il a lui-même redressé.
- `ALCOLOCO_REQUIRE_DB=1` **sans objet, confirmé et non supposé** : `crates/domain/Cargo.toml` a
  un `[dependencies]` vide. Aucun Postgres lancé, gamme `alcoloco-5-b` / 5505 restée libre.
- Hygiène : un seul fichier au diff, `Cargo.toml`/`Cargo.lock`/`SPEC.md`/`web/` byte-identiques,
  **zéro suppression de test**, aucun gate assoupli, aucun `allow` clippy, aucun trailer interdit.

### Les cinq incertitudes de l'auteur — tranchées

1. **Repli Widmark sur âge *ou* taille, y compris pour une femme** : lecture littérale et
   concordante de `SPEC.md` §6.2, §10.0-C et des corps de #5 et #16 — les quatre écrivent
   « âge ou taille ». Chemin par ailleurs inatteignable (§10.0-D rend la date de naissance
   obligatoire). **Rien à corriger.**
2. **`R(0) = INFINITY` quand `a = b = 0`** : la conservation de la dose **est** tenue, par
   `absorbed_alcohol_grams` (rend `A` pour tout τ ≥ 0). Vérifié par mutation. `R` est bruyant,
   pas silencieux ; la PR annonce du **visible**, pas de l'**impossible** — cadrage correct.
3. **`absorbed_alcohol_grams`** : élargissement réel mais pas isolé, cf. MOYEN-2.
4. **« Valeur à mi-montée »** : l'ambiguïté est dans l'énoncé de l'issue, pas dans le code ;
   le test couvre en plus les deux coins τ=m et τ=M, ce qui la neutralise.
5. **`1ac347f` rouge** : reproduit exactement, mais cf. MINEUR-3 — ce rouge prouve moins qu'il
   n'en a l'air.

### Constats non bloquants — où ils partent

| # | Constat | Destination |
|---|---|---|
| MOYEN-1 | Branche « plateau » de `absorbed_alcohol_grams` (`lib.rs:250`) couverte **uniquement** par un test `#[ignore]`, donc jamais par la CI | **#16** |
| MOYEN-2 | Tout le bloc `lib.rs:83-247` est le « Contenu » textuel de #16, pas seulement `absorbed_alcohol_grams` | **#16** |
| MINEUR-3 | Le rouge TDD de `1ac347f` est un `todo!()`, il ne falsifie aucune constante | acté ici, sans suite |
| MINEUR-4 | Seuil fragile `lib.rs:395` : `> 0.1` pour un écart réel de `0.108` (8 % de marge) | **#16** |
| MINEUR-5 | Durées négatives non validées (`lib.rs:180`, `lib.rs:224`) : `a = -1.0` rend 0 en silence | **#16** |
| MINEUR-6 | Doc de module sur `AbsorptionProfile` annoncée mais absente — angle mort déclaré, non atteignable aujourd'hui | **#16** |

### À propager hors code — corps d'issues GitHub encore faux

**Non fait à ce stade : soumis à l'utilisateur** (écriture sur un dépôt public).

- **Issue #5** (fermée), bloc « Mise à jour du 2026-08-17 (PR #38) » : `0,09156`→`0,09516`,
  `45,93`→`45,8202`, `0,346`→`0,347`, rampe `0,221`→`0,222`, Widmark `0,346`→`0,347`,
  exponentiel `0,255`→`0,256`.
- **Issue #16**, ligne `homme : TBW = 2,447 − 0,09156 × âge` → `0,09516`.
- **Issue #37** à vérifier de même.

Le briefing porte déjà le garde-fou (« les corps des issues #5, #16 et #37 recopient l'ancien
coefficient — c'est `SPEC.md` qui fait foi »), donc le risque est couvert même sans correction.
