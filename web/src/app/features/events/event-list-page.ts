import { Component } from '@angular/core';

/** The events the current profile takes part in (SPEC.md §5.2). */
@Component({
  selector: 'app-event-list-page',
  template: `
    <h2>Événements</h2>
    <p>Liste des événements et création d’un nouvel événement.</p>
  `,
})
export class EventListPage {}
